admin_user table ensure no duplicate email entries
db.admin_user.createIndex({email : 1}, {unique : true})

---------
note for unique indexes, don't set backgroud true as its important
db.user.createIndex({ username: 1 }, { unique: true})

----------
for indexes to be used in queries, like fetching news, set background true
----------
fetch published news by date

db.news.find({ publishDate: new Date("Sat, 06 Feb 2016 00:00:00 GMT"), status: 'published' }).sort({status:1, publishDate : 1}).explain('executionStats')

db.news.createIndex({status : 1, publishDate : 1}, {background: true})

----------

